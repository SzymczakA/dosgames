@model IEnumerable<MvcApplication1.Models.Game>
@{
    ViewBag.Title = "Kategoria " + ViewBag.Cat.CategoryEN;
}

<table>
    <tr>
     <td>
       <h2>@ViewBag.Cat.CategoryEN</h2>
     </td>
       </tr>
    <tr>
        <td>
            <div class="gamList">
@foreach (var item in ViewBag.ViewCatGam)
{
                <div onclick="location.href='@Url.Action( "Pobierz", "Gry", new { id=item.Id })'">
                    <img style="height: 160px; width: 160px;" src="../Images/@item.Image" /><br>
                    <b>@item.FullName</b> <br>           
                    <span class="star-rating">
                        <i style="width: 90%;"> </i>
                    </span><br>             
                    Rozmiar: @item.Size MB
                </div>
}
            </div>
        </td>
    </tr>
</table>
